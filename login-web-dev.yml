server:
  port: 9100
  servlet:
    context-path: /login

spring:
  application:
    name: login-web
  
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    service-url:
      defaultZone: http://127.0.0.1:8100/eureka/  

feign:
  hystrix:
    enabled: true
